# Supabase (Required - for DSA problems, auth, comments)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# API base URL (Optional - for duels/backend when using separate API server)
# Leave empty to use same-origin (e.g. Netlify) or localhost:3001 in dev
VITE_API_URL=

# GROQ API (Optional - for Nova chat AI in Netlify functions)
# Add in Netlify UI for production; include here for local `netlify dev`
GROQ_API_KEY=
